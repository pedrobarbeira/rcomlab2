#include "../include/download.h"


int downloadFile(url_t url){

    // int socketfd;
    // int ip[BUFSIZE];
    // if(getIP(url.ip) != 0){
    //     printf("Counldn't get IP\n");
    //     return -1;
    // }

    // printf("IP Address: %s\n", url.ip);

    

    return 0;
}